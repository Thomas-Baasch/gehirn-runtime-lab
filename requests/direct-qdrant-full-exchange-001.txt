Execute direct qdrant-client 1.16.0 as the derived retrieval backend against the unchanged composed Cross-Project Golden Tests GT-01..GT-12 and the unchanged 8/8 recovery/fault-injection gate. Only inject the derived-index class; do not weaken or rewrite test criteria. This is an exchange proof for the derived retrieval role only. No final stack decision.

Evidence rerun: assert at runtime before every reused gate that the injected gate index symbol is exactly governance.direct_qdrant_index.DirectQdrantIndex. Historical static MemOS labels inside reused gate JSON are metadata only and must not be treated as backend evidence.
